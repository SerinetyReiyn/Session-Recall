# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Index database and its WAL sidecars (regenerable from the archive)
data/recall.db
data/recall.db-wal
data/recall.db-shm

# Environment
.env

# Claude.ai account exports: real conversation data, never commit. The ingest
# reads these read-only; they must never enter the (public) repo.
claudia_export/
*.zip
